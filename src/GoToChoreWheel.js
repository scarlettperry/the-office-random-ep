import React from 'react'

const GoToChoreWheel = (props) => {
  // console.log(props);
  return(
    <div>
      <button>Spin the chore wheel!</button>
    </div>
  )
}

export default GoToChoreWheel
